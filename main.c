#ifndef TEST_MODE
int main() {
    restaurarDados();// Fun��o respons�vel por carregar dados salvos anteriormente.
    menu();// Fun��o que exibe o menu principal da aplica��o e lida com as op��es do usu�rio.
  return 0; // Indica que o programa terminou com sucesso.
}
#endif
